version: '3.8'

services:
  app:
    image: YOUR_DOCKER_USERNAME/rental-landing:latest
    container_name: rental-landing
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
